---
name: example-project
description: EXAMPLE — replace with real facts about THIS project: IDs, names, keys-that-aren't-secret.
type: project
---

> ✏️ Example of a `project` memory — the kind of fact that isn't in the code and
> you'd otherwise have to re-explain every time. Edit or delete.

This project automates [what it does]. Key facts worth remembering:

- The main workflow is named "[Workflow Name]" (n8n id `[xxxx]`).
- Customer records live in the `[table]` table; the field for status is `[field]`.
- We decided on [date] to [decision] because [reason]. See [[example-feedback]].

(Never put API keys or secrets here — those go in `.env`.)
